using DG.Tweening;
using UnityEngine;

public class ShipMove : MonoBehaviour
{
    /*public Transform ship;
    //Curved path
    public PathType pathsystem = PathType.CatmullRom;
    public Vector3[] paths = new Vector3[3];
    public Tween t;
    public Ease ease;
    public float duration;
    
    void Start()
    {
        t = ship.transform.DOPath(paths, duration, pathsystem);
        t.SetEase(ease).SetLoops(-1);
    }*/

    /* ship
            .DOMoveY(-1.15f,duration)
            .SetEase(ease)
            .SetLoops(-1);*/
}
